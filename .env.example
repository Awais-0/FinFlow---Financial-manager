# MySQL Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=finflow_db
DB_PORT=3306

# Authentication Secret (use a strong random string)
JWT_SECRET=your_jwt_secret_here_change_in_production

# Optional: Gemini AI API Key (for future AI features)
GEMINI_API_KEY=
